const size = {
    sm: '576px',
    md: '768px',
    lg: '992px',
    xl: '1200px',
  };
  
  export const device = {
    sm: `(min-width: ${size.sm})`,
    md: `(min-width: ${size.md})`,
    lg: `(min-width: ${size.lg})`,
    xl: `(min-width: ${size.xl})`,
    smMax: `(max-width: ${size.sm})`,
    mdMax: `(max-width: ${size.md})`,
    lgMax: `(max-width: ${size.lg})`,
    xlMax: `(max-width: ${size.xl})`,
  };
  